/// The key part of a key-value pair. An alias for [`Vec<u8>`].
pub type Key = Vec<u8>;
/// The value part of a key-value pair. An alias for [`Vec<u8>`].
pub type Val = Vec<u8>;
/// type representing a key-value pair. An alias for (`Vec<u8>`,`Vec<u8>`)
pub type Pair = (Key,Val);
